{
    "vhosts": [
        { "name": "iot" }
    ],
    "permissions": [
        {
            "user": "guest",
            "vhost": "iot",
            "configure": ".*",
            "write": ".*",
            "read": ".*"
        }
    ]
}
